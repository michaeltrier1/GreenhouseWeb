@model IEnumerable<GreenhouseWeb.Models.Greenhouse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Password)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Port)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Password)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Port)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GreenhouseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GreenhouseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GreenhouseID })
        </td>
    </tr>
}

</table>
