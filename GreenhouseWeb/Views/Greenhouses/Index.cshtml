@model IEnumerable<GreenhouseWeb.Models.Greenhouse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Password)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ip)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.portnumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Password)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ip)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.portnumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Greenhouse_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Greenhouse_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Greenhouse_ID })
        </td>
    </tr>
}

</table>
